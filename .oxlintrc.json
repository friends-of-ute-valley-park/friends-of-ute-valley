{
  "rules": {},
  "overrides": [
    {
      "files": ["**/*.vue"],
      "rules": {
        "max-lines": ["error", { "max": 500 }],
        "complexity": ["error", { "max": 80 }]
      }
    }
  ]
}
