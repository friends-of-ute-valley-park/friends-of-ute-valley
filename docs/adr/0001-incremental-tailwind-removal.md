# Incremental Tailwind Removal

We will remove Tailwind incrementally while introducing a semantic CSS design system. Tailwind may temporarily coexist with the new design system so shared recipes, representative pages, and scoped component styles can be migrated and visually verified in small steps before the Tailwind import, Vite plugin, typography plugin, and packages are removed.
