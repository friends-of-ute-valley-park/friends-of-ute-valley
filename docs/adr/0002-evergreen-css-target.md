# Evergreen CSS Target

We target evergreen browsers for the website's CSS rather than preserving compatibility with old or niche browsers. This lets the semantic design system use modern platform features such as cascade layers, container queries, intrinsic layout, `clamp()`, `color-mix()`, logical properties, and relational selectors where they simplify implementation.
