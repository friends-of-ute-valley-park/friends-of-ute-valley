---
export interface Props {
  title: string;
  image: ImageMetadata;
  previewText: string;
  href: string | undefined;
  publishedOn: Date;
  canBeWide?: boolean | undefined;
}

const { title, image, publishedOn, href, previewText } = Astro.props as Props;

import { Image } from "astro:assets";
import { formatShortMonthDayYear, formatShortMonthYear } from "@/utils/date";
---

<a href={"/news/" + href} class="news-teaser">
  <article class="news-teaser__media">
    <Image
      src={image}
      width={800}
      height={500}
      quality="80"
      alt={title}
      class="news-teaser__image"
    />

    <div class="news-teaser__matte"></div>

    <div class="news-teaser__date">
      {
        formatShortMonthDayYear(publishedOn)
      }
    </div>

    <div class="news-teaser__action">
      <span>Read More →</span>
    </div>
  </article>

  <div class="news-teaser__body">
    <div class="news-teaser__meta">
      <span></span>
      <span>{
          formatShortMonthYear(publishedOn)
        }</span>
    </div>
    <h3 class="news-teaser__title">
      {title}
    </h3>
    <p class="news-teaser__preview">
      {previewText}
    </p>
  </div>
</a>

<style>
  .news-teaser {
    display: block;
    color: inherit;
    text-decoration: none;
  }

  .news-teaser__media {
    position: relative;
    aspect-ratio: 16 / 10;
    overflow: hidden;
    border: 1px solid var(--color-border);
    background: var(--color-surface-muted);
    transition: border-color 200ms;
  }

  .news-teaser:hover .news-teaser__media {
    border-color: var(--color-brand-strong);
  }

  :global(.news-teaser__image) {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transform: scale(1.05);
    transition: transform 1000ms;
  }

  .news-teaser:hover :global(.news-teaser__image) {
    transform: scale(1);
  }

  .news-teaser__matte {
    position: absolute;
    inset: 0;
    border: 1rem solid color-mix(in oklab, var(--color-surface) 10%, transparent);
    pointer-events: none;
  }

  .news-teaser__date {
    position: absolute;
    top: 1rem;
    right: 1rem;
    background: var(--color-brand-strong);
    color: var(--color-text-inverse);
    padding: 0.25rem 0.5rem;
    font-family: var(--font-label);
    font-size: var(--text-label);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    font-variant-numeric: tabular-nums;
  }

  .news-teaser__action {
    position: absolute;
    bottom: 1rem;
    left: 1rem;
    border: 1px solid var(--color-border-muted);
    background: color-mix(in oklab, var(--color-surface) 90%, transparent);
    padding: 0.25rem 0.75rem;
    opacity: 0;
    transition: opacity 500ms;
  }

  .news-teaser:hover .news-teaser__action {
    opacity: 1;
  }

  .news-teaser__action span,
  .news-teaser__meta span:last-child {
    font-family: var(--font-label);
    font-size: var(--text-label);
    font-weight: 900;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .news-teaser__action span {
    color: var(--color-brand-strong);
  }

  .news-teaser__body {
    display: grid;
    gap: 0.75rem;
    margin-top: 1.5rem;
  }

  .news-teaser__meta {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    color: var(--color-text-subtle);
    transition: color 200ms;
  }

  .news-teaser__meta span:first-child {
    display: inline-block;
    width: 1.5rem;
    height: 1px;
    background: var(--color-border);
    transition: background-color 200ms;
  }

  .news-teaser:hover .news-teaser__meta {
    color: var(--color-brand-strong);
  }

  .news-teaser:hover .news-teaser__meta span:first-child {
    background: var(--color-brand-strong);
  }

  .news-teaser__title {
    margin: 0;
    color: var(--color-text-strong);
    font-family: var(--font-display);
    font-size: 1.5rem;
    font-weight: 900;
    line-height: var(--leading-tight);
    letter-spacing: 0;
    text-wrap: balance;
    text-transform: uppercase;
    transition: color 200ms;
  }

  .news-teaser:hover .news-teaser__title {
    color: var(--color-brand-strong);
  }

  .news-teaser__preview {
    display: -webkit-box;
    margin: 0;
    overflow: hidden;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 2;
    color: var(--color-text-muted);
    font-size: var(--text-body-small);
    font-weight: 500;
    line-height: var(--leading-body);
    text-wrap: pretty;
  }
</style>
