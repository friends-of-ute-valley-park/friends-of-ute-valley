---
import { getCollection } from 'astro:content';
import InstagramIcon from '~icons/mdi/instagram';
import FacebookIcon from '~icons/mdi/facebook';
import { byPriorityAsc } from '@/utils/content';

interface Props {
  variant?: 'buttons' | 'inline';
}

const { variant = 'buttons' } = Astro.props as Props;
const isInline = variant === 'inline';
const Wrapper = isInline ? 'span' : 'div';

const iconComponents: Record<string, typeof FacebookIcon> = {
  facebook: FacebookIcon,
  instagram: InstagramIcon,
};

const toneClasses: Record<string, string> = {
  facebook: 'link brand',
  instagram: 'link accent',
};

const socialLinks = (await getCollection('social-links'))
  .filter((entry) => !isInline || entry.data.showInline)
  .sort(byPriorityAsc);
---

<Wrapper class:list={['social-links', isInline ? 'social-links is-inline' : 'social-links is-buttons']}>
  {socialLinks.map((entry) => {
    const Icon = iconComponents[entry.data.icon];
    const tone = toneClasses[entry.data.icon] ?? '';
    return (
      <a
        href={entry.data.link}
        class:list={[
          'link',
          isInline ? 'link inline' : 'link button',
          !isInline && tone,
        ]}
        target={isInline ? undefined : '_blank'}
        rel={isInline ? undefined : 'noopener noreferrer nofollow'}
      >
        <span class="label sr-only">{entry.data.title}</span>
        <Icon class={isInline ? 'icon icon inline' : 'icon'} aria-hidden="true" />
      </a>
    );
  })}
</Wrapper>

<style>
  .social-links {
    display: inline-flex;
    align-items: center;
  }

  .social-links.is-inline {
    gap: 0.5rem;
    margin-inline: 0.25rem;
    vertical-align: text-bottom;
  }

  .social-links.is-buttons {
    position: relative;
    gap: 1rem;
  }

  .social-links .link {
    color: inherit;
  }

  .social-links .link.inline {
    color: var(--color-brand-strong);
  }

  .social-links .link.inline:hover {
    color: var(--color-brand-hover);
  }

  .social-links .link.button {
    display: inline-flex;
    min-width: 2.5rem;
    min-height: 2.5rem;
    align-items: center;
    justify-content: center;
    border: 1px solid var(--color-border);
    background: var(--color-surface);
    color: var(--color-text-subtle);
    padding: 0.5rem;
    transition:
      border-color var(--duration-fast),
      color var(--duration-fast),
      transform var(--duration-fast);
  }

  .social-links .link.button:active {
    transform: scale(0.96);
  }

  .social-links .link.brand:hover {
    border-color: var(--color-brand);
    color: var(--color-brand);
  }

  .social-links .link.accent:hover {
    border-color: var(--color-accent-strong);
    color: var(--color-accent-strong);
  }

  .social-links .link.neutral:hover {
    border-color: var(--color-text-strong);
    color: var(--color-text-strong);
  }

  :global(.social-links .icon) {
    width: 1.25rem;
    height: 1.25rem;
  }

  :global(.social-links .icon.inline) {
    width: 1rem;
    height: 1rem;
  }
</style>
